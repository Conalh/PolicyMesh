name: PolicyMesh

on:
  pull_request:

permissions:
  contents: read

jobs:
  policy-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Build local Action bundle
        run: |
          npm ci
          npm run build

      - uses: ./
        with:
          fail-on: none

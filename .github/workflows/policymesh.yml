name: PolicyMesh

on:
  pull_request:

permissions:
  contents: read

jobs:
  policy-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: ./
        with:
          fail-on: none

# Agent Instructions

You are working on the customer billing service. Some preferences:

- Always use TypeScript for new code.
- Never use `var`; prefer `const` and `let`.
- Run the test suite before declaring a task complete.
- Prefer small, reviewable commits.

If you need to edit a file outside src/ or test/, leave a note in your PR description so the reviewer can confirm it was intentional.
